FROM node:18
ARG DB_HOST=75.81.221.38
ARG DB_PORT=5431
ARG DB_USER=postgres
ARG DB_PASSWORD=OmegaA..1154
ARG DB_NAME=aidensrocks

ENV DB_HOST=$DB_HOST
ENV DB_PORT=$DB_PORT
ENV DB_USER=$DB_USER
ENV DB_PASSWORD=$DB_PASSWORD
ENV DB_NAME=$DB_NAME

WORKDIR /app
COPY src/ /app/src
COPY prisma/ /app/prisma
COPY package.json /app
RUN npm install
RUN npx prisma generate