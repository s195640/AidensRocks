vrrp_instance VI_1 {
    state MASTER
    interface ${INTERFACE}
    virtual_router_id 51
    priority ${NODE_PRIORITY}
    advert_int 1

    authentication {
        auth_type PASS
        auth_pass ${VRRP_PASSWORD}
    }

    virtual_ipaddress {
        ${VIP}
    }
}
